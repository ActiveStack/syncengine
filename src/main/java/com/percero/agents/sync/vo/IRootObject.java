package com.percero.agents.sync.vo;

/**
 * A RootObject is one that has no parent object and would otherwise not make it down
 * to a client based on the model tree.  This would typically be TYPE of other Administrative
 * type objects that are used in combo boxes or something like that.
 **/
public interface IRootObject {

}
